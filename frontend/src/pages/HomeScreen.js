import React, { Component } from 'React';
import { View, Text, StyleSheet } from 'react-native';
const { quotes } = require('./src/constants/quotes');
export default class HomeScreen extends Component {
    static navigationOptions = {
        title: 'Welcome',
      };
    render(){
        return (
            <View>
        
            </View>
        );
    }
}
